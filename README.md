# NotifyMe

## Commands

### Local (Node)

- > npm build
- > node bot.js

### Docker

- > docker build -t notify-me .
- > docker run -d --name notify-me-container-2 -p 3000:3000 notify-me
  
Not quite but close

